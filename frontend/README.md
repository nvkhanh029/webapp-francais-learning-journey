# Frontend - deferred

The approved stack remains React + Vite. Frontend Design is not finalized.
This directory intentionally contains no React/Vite implementation, package
manifest, API client, UI mock, or dependency lockfile in scaffold v3.

Backend contributors do not need Node.js, npm, or a running frontend.
Do not initialize or implement this area until the project lead opens Part 5.
When authorized, implement the shared foundation first and commit the actual
package-lock.json generated by npm. Do not reuse unverified v2 package pins.
